on: [push]

jobs:
  hello_world_job:
    runs-on: self-hosted
    name: A job to say hello
    steps:
      - name: Hello world action step
        id: hello
        uses: james-bostock-cx/checkmarx-cxflow-js-github-action@v0.1.1
